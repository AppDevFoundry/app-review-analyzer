# Test Environment Variables
# Copy this file to .env.test and update with your test database credentials

# Test database URL - should point to a separate database from development
# Option 1: Use a separate Neon branch for testing
# DATABASE_URL_TEST="postgresql://user:password@host/test_database?sslmode=require"

# Option 2: Use a local PostgreSQL instance
# DATABASE_URL_TEST="postgresql://postgres:postgres@localhost:5432/app_review_analyzer_test"

# Enable Prisma query logging for debugging
# DEBUG_PRISMA=true
